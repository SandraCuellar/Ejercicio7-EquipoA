const equiposMayoresEdad = (equipos, edad) => {}; //Devuelve una array con los equipos asignados a personas mayores de esa edad
const equiposProvincia = (equipos, provincia) => {}; // Devuelve un array con los equipos asignados a personas de esa provincia
const provincias = (equipos) => {}; //Devuelve una array de provincias donde haya equipos NO SE REPITEN las provincias
const puestos = (equipos) => {}; //Devuelve una array de puestos de trabajadores con equipo asignado
const edadMedia = (equipos) => {}; //Devuelve la media de edad de trabajadores con equipo asignado
const equiposPorEdad = (equipos) => {}; //Devuelve los equipor ordenados por edad de trabajadores con equipo asignado
const equiposTipo = (equipos, tipo) => {}; //Devuelve los equipos de ese tipo
const trabajadoresTipo = (equipos, tipo) => {}; //Devuelve los trabajadores con equipo del equipo asignado
const equiposPorTipo = (equipos) => {}; //Devuelve una array de equipos organizados por tipos y de la forma puesta en el ejercicio (mirar el git del profesor)
const equiposTipoLocalidad = (equipos, tipo, localidad) => {}; // devuelve un array de equipos del tipo especificado y asignados a trabajadores de esa provincia
const resumenEquipos = (equipos) => {}; //Devuelve un array de objetos con la forma especificada (mirar git profesor)
